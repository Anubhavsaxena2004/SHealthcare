# Healthcare AI Chatbot - Environment Configuration Template
# Copy this file as `.env` and fill in your values
# NEVER commit this file to Git!

# ==================== REQUIRED ====================

# OpenAI API Key (Get from https://platform.openai.com/account/api-keys)
# Format: sk-...
OPENAI_API_KEY=sk-your-openai-api-key-here

# ==================== FLASK CONFIGURATION ====================

# Secret key for session management
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-super-secret-random-key-here

# Flask environment (development, testing, production)
FLASK_ENV=development

# Debug mode (True/False) - Only set to True in development!
DEBUG=True

# ==================== DATABASE ====================

# Database URL
# SQLite: sqlite:///users.db
# PostgreSQL: postgresql://user:password@localhost/dbname
# MySQL: mysql://user:password@localhost/dbname
DATABASE_URL=sqlite:///users.db

# ==================== CHATBOT SETTINGS (OPTIONAL) ====================

# OpenAI Model to use
# Options: gpt-4o-mini (default), gpt-4, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Response temperature (0-2)
# 0 = Deterministic, precise answers
# 1 = Balanced (default)
# 2 = Creative, varied answers
OPENAI_TEMPERATURE=0.7

# Maximum tokens in response (higher = longer responses)
OPENAI_MAX_TOKENS=500

# ==================== LOGGING ====================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path (optional)
LOG_FILE=app.log

# ==================== APPLICATION ====================

# Application host (default: localhost)
APP_HOST=localhost

# Application port (default: 5000)
APP_PORT=5000

# Application URL (for generating links)
APP_URL=http://localhost:5000

# ==================== NOTES ====================
# 1. Remove this file before git commit: `git rm .env --cached && git rm .env`
# 2. Add .env to .gitignore if not already there
# 3. For production, use secure environment variable management (AWS Secrets, Azure Key Vault, etc.)
# 4. Rotate API keys regularly
# 5. Never share your OPENAI_API_KEY publicly
